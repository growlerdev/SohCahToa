name: Build and Deploy

on:
  push:
    branches:
      - release/*
      - develop
      - main

permissions:
  contents: write
      
jobs:
        
  Build:
    runs-on: windows-latest

    outputs:
      gitversion_semver: ${{ steps.gitversion.outputs.semver }}
      gitversion_nugetversion: ${{ steps.gitversion.outputs.nugetversion }}

    steps:
    - name: Checkout codegit v
      uses: actions/checkout@v4.1.7
      with:
        fetch-depth: 0

    - name: Setup GitVersion
      uses: gittools/actions/gitversion/setup@v1.2.0
      with:
        versionSpec: '5.x'

    - name: Execute GitVersion
      id: gitversion
      uses: gittools/actions/gitversion/execute@v1.2.0

    # - name: Setup NuGet
    #   uses: NuGet/setup-nuget@v2.0.0

    # - name: NuGet restore
    #   run: nuget restore
      
    # - name: Build DotNet
    #   run: dotnet build -c Release           

    # - name: Pack Projects
    #   run: dotnet pack --output artifacts --configuration Release /p:PackageVersion=${{ steps.gitversion.outputs.nugetversion }}

    # - name: Publish Build Artifacts
    #   uses: actions/upload-artifact@v4.3.4
    #   with:
    #     name: nuget
    #     path: ./artifacts/*.nupkg
    #     retention-days: 1
    #     overwrite: true

    - name: Echo GitVersion SemVer
      run: echo "GitVersion SemVer is ${{ steps.gitversion.outputs.semver }}"
        
  # Deploy_GitHub_Packages:
  #   needs: Build
  #   runs-on: windows-latest
    
  #   steps:

  #   - name: Download a Build Artifact
  #     uses: actions/download-artifact@v4.1.8
  #     with:
  #       name: nuget
  #       path: ./artifacts

  #   - name: Push NuGet Packages
  #     shell: bash
  #     run: |
  #       shopt -s globstar
  #       for PACKAGE_FILE in ./artifacts/*.nupkg; do
  #         echo "Pushing $PACKAGE_FILE"
  #         dotnet nuget push "$PACKAGE_FILE" --api-key ${{ secrets.GHUB_PAT }} --source ${{ secrets.GROWLER_PACKAGES_JSON }}
  #       done

  # Deploy_NuGet-org:
  #   needs: Build
  #   runs-on: windows-latest

  #   steps:

  #   - name: Download a Build Artifact
  #     uses: actions/download-artifact@v4.1.8
  #     with:
  #       name: nuget
  #       path: ./artifacts

  Prepare_Release:
    needs: Build
    runs-on: windows-latest
    outputs:
      commit_messages: ${{ steps.commit_messages.outputs.messages }}
      gitversion_semver: ${{ needs.Build.outputs.gitversion_semver }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.7
        with:
          fetch-depth: 0

      - name: Get commit messages since last release
        id: commit_messages
        run: |
          git log $(git describe --tags --abbrev=0)..HEAD --pretty=format:"%h - %s" > commit_messages.txt
          echo "commit_messages<<EOF" >> $GITHUB_OUTPUT
          cat commit_messages.txt >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

  Create_Release:
    needs: Prepare_Release
    runs-on: windows-latest
    steps:
      - name: Display Version number
        run: echo "version ${{ needs.Prepare_Release.outputs.gitversion_semver }}"

      - name: Create Tag
        uses: negz/create-tag@v1
        with:
          token: ${{ secrets.GHUB_PAT }}
          version: ${{ needs.Prepare_Release.outputs.gitversion_semver }}
          message: ${{ needs.Prepare_Release.outputs.gitversion_semver }}

      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ needs.Prepare_Release.outputs.gitversion_semver }}
          generateReleaseNotes: true
          # artifacts: "release.tar.gz,foo/*.txt"
          # body: |
          #   Description of the release

          #   Commit Messages:
          #   ${{ needs.Prepare_Release.outputs.commit_messages }}

      # - name: Create GitHub Release
      #   uses: actions/create-release@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     tag_name: ${{ needs.Build.outputs.gitversion_semver }}
      #     release_name: ${{ needs.Build.outputs.gitversion_semver }}
      #     draft: false
      #     prerelease: false
      #     body: |
      #       Description of the release

      #       Commit Messages:
      #       ${{ needs.Prepare_Release.outputs.commit_messages }}
          
  # Tag_Repo:
  #   needs: Build
  #   runs-on: windows-latest

  #   outputs:
  #     gitversion_semver: ${{ needs.Build.outputs.gitversion_semver }}

  #   steps:

  #     - name: Display Version number
  #       run: echo "version ${{ needs.Deploy_GitHub_Packages.outputs.gitversion_semver }}"

  #     - name: Create Tag
  #       # You may pin to the exact commit or the version.
  #       # uses: negz/create-tag@39bae1e0932567a58c20dea5a1a0d18358503320
  #       uses: negz/create-tag@v1
  #       with:
  #         # Github Token
  #         token: ${{ secrets.GHUB_PAT }}
  #         # Version (e.g. v0.1.0)
  #         version: ${{ needs.Build.outputs.gitversion_semver }}
  #         # Tag message
  #         message: ${{ needs.Build.outputs.gitversion_semver }}
